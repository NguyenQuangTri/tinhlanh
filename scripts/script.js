//Hàm tự chạy sau khi trang sẵn sàng (hàm ready)
$(function () {
    //Khởi tạo tính năng AOS animation
    AOS.init({
        duration: 1000,
        easing: 'ease-out-back',
        delay: 0,
    });
});